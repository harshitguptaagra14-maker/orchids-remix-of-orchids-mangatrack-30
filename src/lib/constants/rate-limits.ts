export const RATE_LIMITS = {
  MIDDLEWARE: {
    PUBLIC_LIMIT: 30,
    AUTH_LIMIT: 10,
    AUTHENTICATED_LIMIT: 120,
    WINDOW_MS: 60000,
  },
  
  PROGRESS: {
    PER_MINUTE: 10,
    BURST_COUNT: 3,
    BURST_WINDOW_MS: 5000,
  },
  
  STATUS: {
    PER_MINUTE: 5,
    TOGGLE_LIMIT: 3,
    TOGGLE_WINDOW_MS: 300000,
  },
  
  XP: {
    GLOBAL_PER_MINUTE: 10,
    BURST_COUNT: 3,
    BURST_WINDOW_MS: 10000,
  },
  
  API: {
    DEFAULT_LIMIT: 100,
    DEFAULT_WINDOW_MS: 60000,
  },
} as const;

export const MEMORY_STORE_LIMITS = {
  RATE_LIMIT_MAX_ENTRIES: 10000,
  ABUSE_MAX_COUNTERS: 10000,
  ABUSE_MAX_VALUES: 5000,
  CLEANUP_INTERVAL_MS: 5 * 60 * 1000,
  LRU_EVICTION_PERCENT: 0.2,
} as const;

export const EXTERNAL_API_TIMEOUTS = {
  MANGADEX_MS: 15000,
  ANILIST_MS: 15000,
  REDIS_WAIT_MS: 500,
  REDIS_ABUSE_WAIT_MS: 300,
  REDIS_CHAPTER_WAIT_MS: 200,
} as const;

export const DATABASE_TIMEOUTS = {
  DEFAULT_TRANSACTION_MS: 15000,
  LONG_TRANSACTION_MS: 45000,
  HEALTH_CHECK_MS: 5000,
  MAX_WAIT_MS: 5000,
  LONG_MAX_WAIT_MS: 10000,
} as const;
